export const processPayment = async ({
  amount,
  items,
  method
}: {
  amount: number;
  items: any[];
  method: string;
}) => {
  // Use the parameters in implementation
  // e.g., process(amount, items, method)
}; 